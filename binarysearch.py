Class BinarySearch(lists):
      def __init__(self,a=len(list1), b=list1[0]-[1]):
          super().__init__()
      def search(value):
          d = {x:i.count(x) for x==value in list1 }
          low = 0
          high = a
          mid = (low + high)/2
          while low <=high:
              if list1[mid] ==value:
                 return mid
              elif  input[mid]>value:
                  high = mid -1
              elif:
                  low = mid +1
              elif:
                  x == value 
                  return d
                
